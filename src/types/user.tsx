export class User {
  name: string;
  username: string;
  id: number;

  constructor(name: string, username: string, id: number) {
    this.name = name;
    this.username = username;
    this.id = id;
  }
}
