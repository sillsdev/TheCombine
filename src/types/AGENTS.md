# `src/types/writingSystem.ts`

- The items in `uiWritingSystems` should match `localization-ui-badge` in `README.md`.
- The items in `semDomWritingSystems` should match `localization-sd-badge` in `README.md`.
