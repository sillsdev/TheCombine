#!/usr/bin/env bash

for dir in .mypy_cache .tox
do
    if [ -d $dir ] ; then
        rm -rf $dir
    fi
done

python -m pip install --upgrade pip pip-tools
python -m piptools sync dev-requirements.txt
