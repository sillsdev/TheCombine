apiVersion: v1
data:
  COMBINE_JWT_SECRET_KEY: {{ combine_jwt_secret_key | b64encode }}
  COMBINE_SMTP_USERNAME: {{ combine_smtp_username | b64encode }}
  COMBINE_SMTP_PASSWORD: {{ combine_smtp_password | b64encode }}
kind: Secret
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: backend-env-backend
  name: env-backend-secrets
type: Opaque
