---
- name: Create cert proxy client
  import_role:
    name: k8s_make_resources
  vars:
    parent_name: k8s_cert_proxy_client/Create Cert Proxy Client
    k8s_config_dir: "{{ k8s_cert_proxy_cfg }}"
    k8s_templates:
      - env-cert-proxy-configmap.yaml
      - update-cert-oneshot.yaml
      - update-cert-cronjob.yaml
