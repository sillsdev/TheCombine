---

  - name: List .NET Core installation
    command: dotnet --list-runtimes
    register: dotnet_runtimes

  - name: Verify 3.1 is installed; 2.1 is not installed
    assert:
      that:
        - "'Microsoft.AspNetCore.App 2.1' not in dotnet_runtimes.stdout"
        - "'Microsoft.AspNetCore.App 3.1' in dotnet_runtimes.stdout"
