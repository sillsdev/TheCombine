---
- name: Configure Namespaces
  import_role:
    name: k8s_make_resources
  vars:
    parent_name: k8s_namespace/Configure Namespaces
    k8s_config_dir: "{{ k8s_namespace_cfg }}"
    k8s_templates:
      - namespace-combine.yaml
