---
- name: Create Cert Issuers
  import_role:
    name: k8s_make_resources
  vars:
    parent_name: k8s_cert_manager/Create Cert Issuers
    k8s_config_dir: "{{ k8s_cert_mgr_cfg }}"
    k8s_templates:
      - letsencrypt_prod.yaml
      - letsencrypt_staging.yaml
