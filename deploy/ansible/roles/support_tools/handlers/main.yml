---
- name: start display enddate
  systemd:
    name: "{{ item }}"
    state: started
    enabled: true
    daemon_reload: true
  with_items:
    - display-cert-enddate.service
    - display-cert-enddate.timer
