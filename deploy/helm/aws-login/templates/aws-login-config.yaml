---
apiVersion: v1
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: "{{ .Values.awsEcr.configName }}"
  namespace: "{{ .Values.awsEcr.namespace }}"
data:
  NAMESPACES: "{{ .Values.awsEcr.namespace }}"
  PULL_SECRET_NAME: "{{ .Values.awsEcr.pullSecretName }}"
  DOCKER_EMAIL: "{{ .Values.awsEcr.dockerEmail }}"
